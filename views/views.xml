<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="ac_order.list">
      <field name="name">ac_order list</field>
      <field name="model">ac_order.ac_order</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="ac_order.action_window">
      <field name="name">ac_order window</field>
      <field name="res_model">ac_order.ac_order</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="ac_order.action_server">
      <field name="name">ac_order server</field>
      <field name="model_id" ref="model_ac_order_ac_order"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="ac_order" id="ac_order.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="ac_order.menu_1" parent="ac_order.menu_root"/>
    <menuitem name="Menu 2" id="ac_order.menu_2" parent="ac_order.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="ac_order.menu_1_list" parent="ac_order.menu_1"
              action="ac_order.action_window"/>
    <menuitem name="Server to list" id="ac_order" parent="ac_order.menu_2"
              action="ac_order.action_server"/>
    -->
  </data>
</odoo>