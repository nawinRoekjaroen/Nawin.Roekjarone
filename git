git init #เพื่อสร้าง .git 
git add file_name ที่ต้องการใส่
git status    #เพื่อดูสถานะของไฟล์ 
git commit -m "ใส่commit"
git log
git remote add ตั้งชื่อ https:
git remote -v  #เพื่อดู branch ที่สร้าง
git push ชื่อที่ตั้ง ชื่อ branch  #เพื่อเอาไฟล์ไปไว้บน githup

การใช้งาน git
https://saixiii.com/what-is-git/
